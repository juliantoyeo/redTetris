{
  "name": "redtetris",
  "version": "1.0.0",
  "description": "redTetris",
  "main": "",
  "scripts": {
    "start-server": "nodemon --experimental-modules -r esm server/build/main.js",
    "build-server": "babel -d ./server/build ./server/src",
    "install-all": "cd server && yarn install && cd ../client && yarn install && cd .. && yarn install",
    "test": "cd client && npm run test && cd ../server && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juliantoyeo/redTetris.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/juliantoyeo/redTetris/issues"
  },
  "homepage": "https://github.com/juliantoyeo/redTetris#readme",
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.41",
    "@babel/core": "^7.0.0-beta.41",
    "babel-node": "^0.0.1-security"
  },
  "dependencies": {
    "esm": "^3.2.25"
  }
}
